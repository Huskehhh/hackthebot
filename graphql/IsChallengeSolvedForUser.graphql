query IsChallengeSolvedForUser($user_id: Int!, $challenge_id: Int!) {
  htb_solves(where: {user_id: {_eq: $user_id}, challenge_id: {_eq: $challenge_id}}) {
    user_name
    user_id
    solve_type
    id
    challenge_id
  }
}
